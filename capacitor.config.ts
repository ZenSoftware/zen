import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'zen',
  webDir: 'dist/apps/portal',
  bundledWebRuntime: false,
};

export default config;
