version: '3'
name: zen
services:
  postgres:
    image: postgres:14
    container_name: zen
    restart: always
    environment:
      POSTGRES_USER: zenadmin
      POSTGRES_PASSWORD: temp
    volumes:
      - postgres:/var/lib/postgresql/data
    ports:
      - "5445:5432"
volumes:
  postgres:
